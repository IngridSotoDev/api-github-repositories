import styled from "styled-components";

export const Container = styled.footer`
  width: 100%;
  background: var(--yellow);
  padding: 1rem 0;
  position: fixed;
  bottom: 0;

  p {
    text-align: center;
    font-size: 1.4rem;
    color: var(--blue-dark);
  }
`;
